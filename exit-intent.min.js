/** * Exit Intent Popup Module * Handles exit intent detection and Calendly popup display * Used on contact page only */ (function() { 'use strict'; var EXIT_CONFIG = { MOUSE_VELOCITY_THRESHOLD: 50, MOUSE_TOP_POSITION_PX: 100, MOBILE_SWIPE_THRESHOLD_PX: 100, SCROLL_TOP_THRESHOLD_PX: 50 }; document.addEventListener('DOMContentLoaded', function() { var exitPopup = document.getElementById('exitPopup'); var skipFormBtn = document.getElementById('skipFormBtn'); if (!exitPopup) return; var popupShown = false; var SESSION_KEY = 'exitPopupShown'; function wasPopupShown() { try { return sessionStorage.getItem(SESSION_KEY) === 'true'; } catch (e) { return false; } } function markPopupShown() { try { sessionStorage.setItem(SESSION_KEY, 'true'); } catch (e) { // sessionStorage unavailable } } function showPopup(bypassSession) { if (popupShown || (!bypassSession && wasPopupShown())) return; popupShown = true; markPopupShown(); exitPopup.classList.add('visible'); document.body.style.overflow = 'hidden'; } function hidePopup() { exitPopup.classList.remove('visible'); document.body.style.overflow = ''; } // Skip button click - always shows popup (bypasses session check) if (skipFormBtn) { skipFormBtn.addEventListener('click', function() { popupShown = false; showPopup(true); }); } // EXIT INTENT: Mouse leaving toward top of page document.addEventListener('mouseout', function(e) { var isNearTop = e.clientY <= 10; var isLeavingPage = !e.relatedTarget || e.relatedTarget.nodeName === 'HTML'; if (isNearTop && isLeavingPage) { showPopup(false); } }); // EXIT INTENT: Fast upward mouse movement var lastY = 0; document.addEventListener('mousemove', function(e) { var velocity = lastY - e.clientY; lastY = e.clientY; if (velocity > EXIT_CONFIG.MOUSE_VELOCITY_THRESHOLD && e.clientY < EXIT_CONFIG.MOUSE_TOP_POSITION_PX) { showPopup(false); } }); // Mobile: Pull-to-refresh gesture detection var touchStartY = 0; document.addEventListener('touchstart', function(e) { touchStartY = e.touches[0].clientY; }, { passive: true }); document.addEventListener('touchend', function(e) { var touchEndY = e.changedTouches[0].clientY; var scrollTop = window.pageYOffset || document.documentElement.scrollTop; if (scrollTop < EXIT_CONFIG.SCROLL_TOP_THRESHOLD_PX && touchEndY - touchStartY > EXIT_CONFIG.MOBILE_SWIPE_THRESHOLD_PX) { showPopup(false); } }, { passive: true }); // Close handlers var closeBtn = exitPopup.querySelector('.exit-popup-close'); var dismissBtn = exitPopup.querySelector('.exit-popup-dismiss'); if (closeBtn) { closeBtn.addEventListener('click', hidePopup); } if (dismissBtn) { dismissBtn.addEventListener('click', hidePopup); } exitPopup.addEventListener('click', function(e) { if (e.target === exitPopup) hidePopup(); }); document.addEventListener('keydown', function(e) { if (e.key === 'Escape') hidePopup(); }); }); // Talk to Human - Computer Form Toggle (contact page specific) document.addEventListener('DOMContentLoaded', function() { var showComputerFormBtn = document.getElementById('showComputerForm'); var backToOptionsBtn = document.getElementById('backToOptions'); var computerForm = document.getElementById('computerForm'); var talkHumanOptions = document.getElementById('talkHumanOptions'); var talkHumanForm = document.getElementById('talkHumanForm'); // Store original form HTML for reset var originalFormHTML = computerForm ? computerForm.innerHTML : ''; if (showComputerFormBtn && computerForm && talkHumanOptions) { showComputerFormBtn.addEventListener('click', function() { talkHumanOptions.hidden = true; computerForm.hidden = false; }); } if (backToOptionsBtn && computerForm && talkHumanOptions) { backToOptionsBtn.addEventListener('click', function() { computerForm.hidden = true; talkHumanOptions.hidden = false; }); } // Form submission if (talkHumanForm) { talkHumanForm.addEventListener('submit', function(e) { e.preventDefault(); var formData = new FormData(talkHumanForm); var submitBtn = talkHumanForm.querySelector('.talk-human-send'); var originalText = submitBtn ? submitBtn.textContent : 'Send'; if (submitBtn) { submitBtn.disabled = true; submitBtn.textContent = 'Sending...'; } fetch('https://api.web3forms.com/submit', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ access_key: '1b112abb-70d7-4e79-a0af-5d0b3e425015', name: formData.get('name'), phone: formData.get('phone'), message: formData.get('message'), subject: 'Talk to Human - SMS Request' }) }) .then(function(response) { if (response.ok) { computerForm.innerHTML = '<div style="text-align: center; padding: 20px 0;">' + '<div style="width: 60px; height: 60px; background: linear-gradient(135deg, #25D366 0%, #128C7E 100%); border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 16px;">' + '<svg width="30" height="30" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg>' + '</div>' + '<h3 style="font-size: 18px; font-weight: 700; color: #0A1927; margin-bottom: 8px;">Message Received!</h3>' + '<p style="font-size: 14px; color: #666; line-height: 1.5;">We\'ll text you back within 5 minutes during business hours (8am-6pm EST).</p>' + '</div>'; } else { throw new Error('Failed'); } }) .catch(function() { if (submitBtn) { submitBtn.textContent = 'Error - Try Again'; submitBtn.disabled = false; setTimeout(function() { submitBtn.textContent = originalText; }, 2000); } }); }); } // Store reset function for modal close window._resetContactForm = function() { if (computerForm && originalFormHTML) { computerForm.innerHTML = originalFormHTML; computerForm.hidden = true; } if (talkHumanOptions) { talkHumanOptions.hidden = false; } }; }); })(); 
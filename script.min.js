document.addEventListener('DOMContentLoaded', () => {
const cookieBanner = document.querySelector('.cookie-banner');
const cookieAccept = document.querySelector('.cookie-accept');
const cookieDecline = document.querySelector('.cookie-decline');
if (cookieBanner) {
const cookieChoice = localStorage.getItem('acquileadz_cookies');
if (!cookieChoice) {
setTimeout(() => {
cookieBanner.removeAttribute('hidden');
cookieBanner.classList.add('visible');
}, 1000);
}
if (cookieAccept) {
cookieAccept.addEventListener('click', () => {
localStorage.setItem('acquileadz_cookies', 'all');
hideCookieBanner();
});
}
if (cookieDecline) {
cookieDecline.addEventListener('click', () => {
localStorage.setItem('acquileadz_cookies', 'essential');
hideCookieBanner();
});
}
function hideCookieBanner() {
cookieBanner.classList.remove('visible');
setTimeout(() => {
cookieBanner.setAttribute('hidden', '');
}, 300);
}
}
const mobileMenuToggle = document.querySelector('.mobile-menu-toggle');
const navLinks = document.querySelector('.nav-links');
if (mobileMenuToggle) {
mobileMenuToggle.addEventListener('click', () => {
const isExpanded = mobileMenuToggle.classList.toggle('active');
navLinks.classList.toggle('active');
mobileMenuToggle.setAttribute('aria-expanded', isExpanded);
});
document.querySelectorAll('.nav-links a').forEach(link => {
link.addEventListener('click', () => {
mobileMenuToggle.classList.remove('active');
navLinks.classList.remove('active');
mobileMenuToggle.setAttribute('aria-expanded', 'false');
});
});
document.addEventListener('click', (e) => {
if (!e.target.closest('.nav') && navLinks.classList.contains('active')) {
mobileMenuToggle.classList.remove('active');
navLinks.classList.remove('active');
mobileMenuToggle.setAttribute('aria-expanded', 'false');
}
});
}
let navTicking = false;
window.addEventListener('scroll', () => {
if (!navTicking) {
window.requestAnimationFrame(() => {
const nav = document.querySelector('.nav');
if (window.pageYOffset > 50) {
nav.classList.add('scrolled');
} else {
nav.classList.remove('scrolled');
}
navTicking = false;
});
navTicking = true;
}
}, { passive: true });
const dashboard = document.getElementById('dashboard3d');
if (dashboard) {
document.addEventListener('mousemove', (e) => {
const x = (e.clientX / window.innerWidth) - 0.5;
const y = (e.clientY / window.innerHeight) - 0.5;
const rotateX = y * -6;
const rotateY = x * 8;
dashboard.style.transform = `rotateX(${rotateX}deg) rotateY(${rotateY}deg) translateZ(10px)`;
});
document.addEventListener('mouseleave', () => {
dashboard.style.transition = 'transform 0.5s ease-out';
dashboard.style.transform = 'rotateX(0deg) rotateY(0deg) translateZ(0px)';
setTimeout(() => { dashboard.style.transition = ''; }, 500);
});
}
initGSAPAnimations();
});
function initGSAPAnimations() {
if (typeof gsap === 'undefined' || typeof ScrollTrigger === 'undefined') {
console.log('GSAP not loaded - skipping animations');
return;
}
gsap.registerPlugin(ScrollTrigger);
gsap.utils.toArray('.metric').forEach((metric, i) => {
gsap.fromTo(metric,
{
opacity: 0,
y: 50
},
{
opacity: 1,
y: 0,
duration: 1,
ease: 'power2.out',
scrollTrigger: {
trigger: metric,
start: 'top 85%',
end: 'top 50%',
scrub: 1
}
}
);
});
gsap.utils.toArray('.value-box').forEach((box, i) => {
gsap.fromTo(box,
{
opacity: 0,
y: 50
},
{
opacity: 1,
y: 0,
duration: 1,
ease: 'power2.out',
scrollTrigger: {
trigger: box,
start: 'top 85%',
end: 'top 50%',
scrub: 1
}
}
);
});
gsap.utils.toArray('.feature-card').forEach((card, i) => {
gsap.fromTo(card,
{
opacity: 0,
y: 50
},
{
opacity: 1,
y: 0,
duration: 1,
ease: 'power2.out',
scrollTrigger: {
trigger: card,
start: 'top 85%',
end: 'top 50%',
scrub: 1
}
}
);
});
gsap.utils.toArray('.testimonial-card').forEach((card, i) => {
gsap.fromTo(card,
{
opacity: 0,
y: 40
},
{
opacity: 1,
y: 0,
duration: 1,
ease: 'power2.out',
scrollTrigger: {
trigger: card,
start: 'top 90%',
end: 'top 60%',
scrub: 1
}
}
);
});
gsap.utils.toArray('.service-detail').forEach((card, i) => {
gsap.fromTo(card,
{
opacity: 0,
y: 50
},
{
opacity: 1,
y: 0,
duration: 1,
ease: 'power2.out',
scrollTrigger: {
trigger: card,
start: 'top 85%',
end: 'top 50%',
scrub: 1
}
}
);
});
gsap.utils.toArray('.problem-item').forEach((item, i) => {
gsap.fromTo(item,
{
opacity: 0,
x: -30
},
{
opacity: 1,
x: 0,
duration: 1,
ease: 'power2.out',
scrollTrigger: {
trigger: item,
start: 'top 85%',
end: 'top 60%',
scrub: 1
}
}
);
});
gsap.utils.toArray('.benefit-item').forEach((item, i) => {
gsap.fromTo(item,
{
opacity: 0,
y: 20
},
{
opacity: 1,
y: 0,
duration: 1,
ease: 'power2.out',
scrollTrigger: {
trigger: item,
start: 'top 90%',
end: 'top 70%',
scrub: 1
}
}
);
});
gsap.utils.toArray('.offering-card').forEach((card, i) => {
gsap.fromTo(card,
{
opacity: 0,
y: 40
},
{
opacity: 1,
y: 0,
duration: 1,
ease: 'power2.out',
scrollTrigger: {
trigger: card,
start: 'top 85%',
end: 'top 55%',
scrub: 1
}
}
);
});
gsap.utils.toArray('.process-step').forEach((step, i) => {
gsap.fromTo(step,
{
opacity: 0,
y: 30
},
{
opacity: 1,
y: 0,
duration: 1,
ease: 'power2.out',
scrollTrigger: {
trigger: step,
start: 'top 85%',
end: 'top 60%',
scrub: 1
}
}
);
});
gsap.utils.toArray('.faq-item').forEach((item, i) => {
gsap.fromTo(item,
{
opacity: 0,
y: 30
},
{
opacity: 1,
y: 0,
duration: 1,
ease: 'power2.out',
scrollTrigger: {
trigger: item,
start: 'top 90%',
end: 'top 65%',
scrub: 1
}
}
);
});
gsap.utils.toArray('.pricing-item').forEach((item, i) => {
gsap.fromTo(item,
{
opacity: 0,
y: 20
},
{
opacity: 1,
y: 0,
duration: 1,
ease: 'power2.out',
scrollTrigger: {
trigger: item,
start: 'top 90%',
end: 'top 70%',
scrub: 1
}
}
);
});
gsap.utils.toArray('.value-item').forEach((item, i) => {
gsap.fromTo(item,
{
opacity: 0,
y: 30
},
{
opacity: 1,
y: 0,
duration: 1,
ease: 'power2.out',
scrollTrigger: {
trigger: item,
start: 'top 85%',
end: 'top 60%',
scrub: 1
}
}
);
});
gsap.utils.toArray('.trait-item').forEach((item, i) => {
gsap.fromTo(item,
{
opacity: 0,
x: -20
},
{
opacity: 1,
x: 0,
duration: 1,
ease: 'power2.out',
scrollTrigger: {
trigger: item,
start: 'top 90%',
end: 'top 70%',
scrub: 1
}
}
);
});
gsap.utils.toArray('.proof-stat').forEach((stat, i) => {
gsap.fromTo(stat,
{
opacity: 0,
y: 30
},
{
opacity: 1,
y: 0,
duration: 1,
ease: 'power2.out',
scrollTrigger: {
trigger: stat,
start: 'top 85%',
end: 'top 60%',
scrub: 1
}
}
);
});
gsap.utils.toArray('.team-card').forEach((card, i) => {
gsap.fromTo(card,
{
opacity: 0,
y: 40
},
{
opacity: 1,
y: 0,
duration: 1,
ease: 'power2.out',
scrollTrigger: {
trigger: card,
start: 'top 85%',
end: 'top 55%',
scrub: 1
}
}
);
});
gsap.utils.toArray('.about-card').forEach((card, i) => {
gsap.fromTo(card,
{
opacity: 0,
y: 50
},
{
opacity: 1,
y: 0,
duration: 1,
ease: 'power2.out',
scrollTrigger: {
trigger: card,
start: 'top 85%',
end: 'top 50%',
scrub: 1
}
}
);
});
gsap.utils.toArray('.diff-card').forEach((card, i) => {
gsap.fromTo(card,
{
opacity: 0,
y: 30
},
{
opacity: 1,
y: 0,
duration: 1,
ease: 'power2.out',
scrollTrigger: {
trigger: card,
start: 'top 85%',
end: 'top 60%',
scrub: 1
}
}
);
});
const finalCta = document.querySelector('.final-cta-content');
if (finalCta) {
gsap.fromTo(finalCta,
{
opacity: 0,
y: 40
},
{
opacity: 1,
y: 0,
duration: 1,
ease: 'power2.out',
scrollTrigger: {
trigger: finalCta,
start: 'top 80%',
end: 'top 50%',
scrub: 1
}
}
);
}
initProcessFlowAnimations();
}
function initProcessFlowAnimations() {
const flowSteps = document.querySelectorAll('.flow-step');
if (flowSteps.length === 0) return;
flowSteps.forEach((step, index) => {
const stepBox = step.querySelector('.step-box');
const title = step.querySelector('.animated-text');
const description = step.querySelector('.step-box p');
const arrowConnector = step.querySelector('.arrow-connector');
const arrowPath = step.querySelector('.arrow-path');
const arrowHead = step.querySelector('.arrow-head');
const successBadge = step.querySelector('.success-badge');
const tl = gsap.timeline({
scrollTrigger: {
trigger: step,
start: 'top 80%',
end: 'top 30%',
scrub: 1
}
});
if (stepBox) {
gsap.set(stepBox, { opacity: 0, y: 60, scale: 0.95 });
tl.to(stepBox, {
opacity: 1,
y: 0,
scale: 1,
duration: 0.5,
ease: 'power2.out'
}, 0);
}
if (title) {
gsap.set(title, { opacity: 0, y: 20 });
tl.to(title, {
opacity: 1,
y: 0,
duration: 0.3,
ease: 'power2.out'
}, 0.1);
}
if (description) {
gsap.set(description, { opacity: 0, y: 15 });
tl.to(description, {
opacity: 1,
y: 0,
duration: 0.3,
ease: 'power2.out'
}, 0.2);
}
if (arrowPath) {
const pathLength = arrowPath.getTotalLength ? arrowPath.getTotalLength() : 150;
gsap.set(arrowPath, {
strokeDasharray: pathLength,
strokeDashoffset: pathLength
});
tl.to(arrowPath, {
strokeDashoffset: 0,
duration: 0.4,
ease: 'none'
}, 0.3);
}
if (arrowHead) {
gsap.set(arrowHead, { opacity: 0, scale: 0.5 });
tl.to(arrowHead, {
opacity: 1,
scale: 1,
duration: 0.2,
ease: 'back.out(1.7)'
}, 0.6);
}
if (successBadge) {
const circle = successBadge.querySelector('.success-circle');
const check = successBadge.querySelector('.success-check');
if (circle) {
gsap.set(circle, {
strokeDasharray: 176,
strokeDashoffset: 176
});
tl.to(circle, {
strokeDashoffset: 0,
duration: 0.4,
ease: 'power2.out'
}, 0.4);
}
if (check) {
gsap.set(check, {
strokeDasharray: 60,
strokeDashoffset: 60
});
tl.to(check, {
strokeDashoffset: 0,
duration: 0.3,
ease: 'power2.out'
}, 0.6);
}
}
});
}
document.addEventListener('DOMContentLoaded', () => {
const form = document.querySelector('.contact-form');
if (!form) return;
const result = document.getElementById('result');
const emailInput = form.querySelector('input[name="email"]');
const phoneInput = form.querySelector('input[name="phone"]');
const RATE_LIMIT = {
MAX_SUBMISSIONS: 3,
TIME_WINDOW_MS: 60000,
STORAGE_KEY: 'form_submissions'
};
function checkRateLimit() {
try {
const submissions = JSON.parse(localStorage.getItem(RATE_LIMIT.STORAGE_KEY) || '[]');
const now = Date.now();
const recentSubmissions = submissions.filter(time => now - time < RATE_LIMIT.TIME_WINDOW_MS);
if (recentSubmissions.length >= RATE_LIMIT.MAX_SUBMISSIONS) {
const oldestSubmission = Math.min(...recentSubmissions);
const waitTime = Math.ceil((RATE_LIMIT.TIME_WINDOW_MS - (now - oldestSubmission)) / 1000);
return { allowed: false, waitTime };
}
return { allowed: true };
} catch (e) {
return { allowed: true };
}
}
function recordSubmission() {
try {
const submissions = JSON.parse(localStorage.getItem(RATE_LIMIT.STORAGE_KEY) || '[]');
const now = Date.now();
submissions.push(now);
const recentSubmissions = submissions.filter(time => now - time < RATE_LIMIT.TIME_WINDOW_MS);
localStorage.setItem(RATE_LIMIT.STORAGE_KEY, JSON.stringify(recentSubmissions));
} catch (e) {
}
}
function isValidEmail(email) {
if (!email || email.length > 320) return false;
const emailRegex = /^[a-zA-Z0-9.!#$%&'*+\/=?^_`{|}~-]+@[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)*$/;
if (!emailRegex.test(email)) return false;
const suspiciousPatterns = [
/\.\./,
/^\./,
/\.$/,
/@\./,
/\.@/
];
return !suspiciousPatterns.some(pattern => pattern.test(email));
}
function isValidPhone(phone) {
if (!phone) return false;
const digitsOnly = phone.replace(/\D/g, '');
if (digitsOnly.length < 10 || digitsOnly.length > 15) return false;
if (/^(.)\1+$/.test(digitsOnly)) return false;
return true;
}
function showMessage(message, type) {
if (!result) return;
result.textContent = message;
result.style.display = 'block';
if (type === 'success') {
result.style.backgroundColor = '#d4edda';
result.style.color = '#155724';
result.style.border = '1px solid #c3e6cb';
} else if (type === 'error') {
result.style.backgroundColor = '#f8d7da';
result.style.color = '#721c24';
result.style.border = '1px solid #f5c6cb';
} else {
result.style.backgroundColor = '#d1ecf1';
result.style.color = '#0c5460';
result.style.border = '1px solid #bee5eb';
}
}
form.addEventListener('submit', async function(e) {
e.preventDefault();
const rateLimitCheck = checkRateLimit();
if (!rateLimitCheck.allowed) {
showMessage(`Too many submissions. Please wait ${rateLimitCheck.waitTime} seconds before trying again.`, 'error');
return;
}
if (!isValidEmail(emailInput.value)) {
showMessage('Please enter a valid email address.', 'error');
emailInput.focus();
return;
}
if (!isValidPhone(phoneInput.value)) {
showMessage('Please enter a valid phone number (at least 10 digits).', 'error');
phoneInput.focus();
return;
}
const formData = new FormData(form);
const submitButton = form.querySelector('button[type="submit"]');
const originalButtonText = submitButton?.innerHTML || 'Send Message';
try {
if (submitButton) {
submitButton.disabled = true;
submitButton.innerHTML = 'Sending...';
}
showMessage('Sending your message...', 'info');
const response = await fetch('https://api.web3forms.com/submit', {
method: 'POST',
headers: {
'Content-Type': 'application/json',
'Accept': 'application/json'
},
body: JSON.stringify(Object.fromEntries(formData))
});
const data = await response.json();
if (response.ok && data.success) {
showMessage('✓ Message sent successfully! Redirecting...', 'success');
form.reset();
recordSubmission();
if (typeof sessionStorage !== 'undefined') {
sessionStorage.setItem('exitPopupDismissed', 'true');
}
setTimeout(() => {
window.location.href = 'success.html';
}, 800);
} else {
throw new Error(data.message || 'Form submission failed');
}
} catch (error) {
console.error('Form submission error:', error);
showMessage('Something went wrong. Please try again or email us directly at hello@acquileadz.com', 'error');
} finally {
if (submitButton) {
submitButton.disabled = false;
submitButton.innerHTML = originalButtonText;
}
setTimeout(() => {
if (result) result.style.display = 'none';
}, 8000);
}
});
});
document.addEventListener('DOMContentLoaded', () => {
const teamModal = document.getElementById('teamModal');
if (!teamModal) return;
const teamCards = document.querySelectorAll('.team-card');
const modalClose = document.querySelector('.team-modal-close');
const teamData = {
'John Gartland': {
photo: 'assets/john-gartland.png',
name: 'John Gartland',
role: 'Founder & Operations',
email: 'john@acquileadz.com',
bio: `<p>John founded Acquileadz with a singular mission: help contractors stop guessing and start growing. With extensive experience in outreach, copywriting, and digital marketing across multiple online businesses, he's built systems that turn chaos into consistency.</p>
<p>Before Acquileadz, John worked hands-on with online-based businesses, managing everything from mass outreach campaigns to social media marketing. He built websites, managed sales pipelines, and crafted copy that converted—skills that now power every contractor system at Acquileadz.</p>
<p>Now, John runs the day-to-day operations at Acquileadz, ensuring every client gets a customized system that actually works. He's known for his direct communication style, obsessive attention to detail, and refusal to let leads slip through the cracks.</p>`,
skills: ['Digital Marketing', 'Copywriting', 'Client Relations', 'Pipeline Management', 'Website Development', 'Operations Management'],
achievements: [
'Built and managed high-conversion sales pipelines for multiple online businesses',
'Executed mass outreach campaigns generating thousands of qualified leads',
'Developed websites and digital systems that streamlined business operations',
'Crafted copywriting and marketing content that drove client acquisition',
'Managed social media marketing strategies across multiple platforms'
]
},
'Luke McCloskey': {
photo: 'assets/Luke.png',
name: 'Luke McCloskey',
role: 'Co-Founder & Strategy',
email: 'luke@acquileadz.com',
bio: `<p>Luke is the strategic mind behind Acquileadz's marketing systems. He's spent years perfecting the art of turning ad spend into booked jobs, and he's obsessed with making sure contractors stay ahead of their competition.</p>
<p>With a background in digital marketing and conversion optimization, Luke has managed millions in ad spend across dozens of contractor campaigns. He knows what works, what doesn't, and—more importantly—why.</p>
<p>At Acquileadz, Luke builds the campaigns, optimizes the funnels, and ensures every dollar spent is working harder than the last. He's constantly testing new strategies, analyzing data, and finding the edge that keeps contractors booked solid.</p>`,
skills: ['Campaign Strategy', 'Funnel Optimization', 'Digital Marketing', 'Conversion Rate Optimization', 'Analytics & Testing', 'Competitive Analysis'],
achievements: [
'Managed over $2M in contractor advertising spend with average 3.2x ROI',
'Developed conversion-optimized landing page templates with 40%+ conversion rates',
'Launched and scaled 50+ contractor campaigns across multiple trades',
'Created data-driven attribution models that increased lead quality by 35%'
]
}
};
teamCards.forEach(card => {
card.addEventListener('click', () => {
const memberName = card.querySelector('h3').textContent;
const data = teamData[memberName];
if (data) {
document.getElementById('modalPhoto').src = data.photo;
document.getElementById('modalPhoto').alt = data.name;
document.getElementById('modalName').textContent = data.name;
document.getElementById('modalRole').textContent = data.role;
document.getElementById('modalEmail').href = `mailto:${data.email}`;
document.getElementById('modalEmail').textContent = data.email;
document.getElementById('modalBio').innerHTML = data.bio;
const skillsContainer = document.getElementById('modalSkills');
skillsContainer.innerHTML = data.skills.map(skill =>
`<div class="skill-badge">${skill}</div>`
).join('');
const achievementsContainer = document.getElementById('modalAchievements');
achievementsContainer.innerHTML = data.achievements.map(achievement =>
`<li>${achievement}</li>`
).join('');
teamModal.classList.add('active');
document.body.style.overflow = 'hidden';
}
});
});
function closeModal() {
teamModal.classList.remove('active');
document.body.style.overflow = '';
}
if (modalClose) {
modalClose.addEventListener('click', closeModal);
}
teamModal.addEventListener('click', (e) => {
if (e.target === teamModal) {
closeModal();
}
});
document.addEventListener('keydown', (e) => {
if (e.key === 'Escape' && teamModal.classList.contains('active')) {
closeModal();
}
});
});